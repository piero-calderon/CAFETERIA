/* =========================================================================
   MAIN.JS
   Punto de entrada / inicializaciones generales.
   Este archivo está reservado para inicializaciones globales que no
   pertenecen a un módulo específico (preloader, navbar, scroll, animations).
   Se carga al final para asegurar que todos los módulos previos
   estén disponibles.
   ========================================================================= */

/* -------------------------------------------------------------------------
   Espacio reservado para inicializaciones globales adicionales.
   ------------------------------------------------------------------------- */
